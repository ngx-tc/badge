import { size } from '@ngx-tc/base';

export enum badgeArrow {
  top = 'TOP',
  right = 'RIGHT',
  bottom = 'BOTTOM',
  left = 'LEFT'
}

export enum badgeSize {
  xs = 'XS',
  sm = 'SM',
  default = 'DEFAULT',
  lg = 'LG'
}
